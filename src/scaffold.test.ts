import { describe, expect, it } from "vitest";

// Placeholder proving the test runner is wired up (backlog item 001).
// Replace with real suites starting at backlog item 005.
describe("scaffold", () => {
  it("runs the test suite", () => {
    expect(1 + 1).toBe(2);
  });
});
