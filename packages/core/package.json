{
  "name": "@kestrel/core",
  "version": "0.1.0",
  "private": true,
  "description": "Kestrel's pure domain: shared types, config, metrics, screens, and the storage seam.",
  "type": "module",
  "exports": {
    "./config": "./src/config/index.ts",
    "./types": "./src/types/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "biome check ."
  }
}
