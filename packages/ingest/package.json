{
  "name": "@kestrel/ingest",
  "version": "0.1.0",
  "private": true,
  "description": "Kestrel's ingestion worker library: provider adapters, capability registry, and the throttled daily/backfill pipeline.",
  "type": "module",
  "exports": {
    "./*": "./src/*.ts"
  },
  "dependencies": {
    "@kestrel/core": "workspace:*"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "biome check ."
  }
}
