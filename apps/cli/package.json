{
  "name": "@kestrel/cli",
  "version": "0.1.0",
  "private": true,
  "description": "Kestrel's composition root: the daily-pipeline CLI and text dashboard.",
  "type": "module",
  "dependencies": {
    "@kestrel/core": "workspace:*",
    "@kestrel/ingest": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "scripts": {
    "build": "esbuild src/app/cli.ts --bundle --platform=node --target=node22 --format=esm --outfile=dist/cli.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "biome check ."
  }
}
