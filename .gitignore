node_modules/
dist/
coverage/
*.tsbuildinfo
.turbo/
.next/
.vercel/
# Local SQLite scratch DB (the reference engine is in-memory for tests;
# nothing in the deployed app writes here). Retired daily-run outputs.
data/
dashboard.md
# Generated by `next build`/`next dev`; references build output
# (.next/types), so committing it would break clean-checkout typechecks.
next-env.d.ts
